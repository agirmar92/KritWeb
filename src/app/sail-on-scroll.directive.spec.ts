import { SailOnScrollDirective } from './sail-on-scroll.directive';

describe('SailOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SailOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
