import { PanOnScrollDirective } from './pan-on-scroll.directive';

describe('PanOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PanOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
